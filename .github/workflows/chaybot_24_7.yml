name: Ping healthz
on:
    schedule:
        - cron: '*/5 * * * *'  # Runs every 5 minutes
    workflow_dispatch:
jobs:
    ping:
        runs-on: ubuntu-latest
        steps:
            - name: Ping the website
              run: |
                curl -fsS --max-time 10 https://bot-24-7-2-e9u8.onrender.com/healthz || echo "PING FAILED"